export enum PatientStatus {
	pending = "pending",
	approved = "approved",
	complete = "complete",
}

export enum AppointmentStatus {
	pending = "pending",
	approved = "approved",
	complete = "complete",
}